# دليل استكشاف الأخطاء - Troubleshooting Guide

## مشكلة: "حدث خطأ في الاتصال بالخدمة"

### الأسباب المحتملة:

#### 1. 🔑 مشكلة في مفتاح API
```bash
# تحقق من وجود مفتاح API
- تأكد من إدخال مفتاح Gemini API صحيح
- تحقق من أن المفتاح لم ينته صلاحيته
- تأكد من تفعيل API في Google AI Studio
```

#### 2. 🌐 مشكلة في الاتصال بالإنترنت
```bash
# تحقق من الاتصال
- تأكد من اتصال الإنترنت
- جرب إعادة تحميل الصفحة
- تحقق من إعدادات الجدار الناري
```

#### 3. ⚡ مشكلة في الخادم
```bash
# إعادة تشغيل الخادم
cd frontend
npm run dev
```

#### 4. 📊 تجاوز الحد المسموح
```bash
# إذا ظهرت رسالة "quota exceeded"
- انتظر دقيقة ثم حاول مرة أخرى
- تحقق من حدود API في Google AI Studio
- استخدم مفتاح API آخر إذا لزم الأمر
```

### الحلول السريعة:

#### الحل 1: إعادة تشغيل الخادم
```powershell
# في PowerShell
cd frontend
npm run dev
```

#### الحل 2: مسح ذاكرة التخزين المؤقت
```javascript
// في متصفح الويب
localStorage.clear();
sessionStorage.clear();
// ثم أعد تحميل الصفحة
```

#### الحل 3: التحقق من مفتاح API
1. اذهب إلى [Google AI Studio](https://makersuite.google.com/app/apikey)
2. تحقق من صحة المفتاح
3. تأكد من تفعيل Gemini API
4. أعد إدخال المفتاح في التطبيق

#### الحل 4: استخدام مفتاح API احتياطي
```javascript
// في الإعدادات، جرب مفتاح API آخر
// أو استخدم مفتاح API من حساب Google آخر
```

### رسائل الخطأ الشائعة:

| رسالة الخطأ | السبب | الحل |
|-------------|-------|------|
| "مفتاح API غير صحيح" | مفتاح API خاطئ | تحقق من المفتاح في Google AI Studio |
| "تم استنفاذ الحد المسموح" | تجاوز حدود API | انتظر أو استخدم مفتاح آخر |
| "TIMEOUT" | انقطاع الاتصال | تحقق من الإنترنت وأعد المحاولة |
| "Method not allowed" | خطأ في الطلب | أعد تحميل الصفحة |
| "حدث خطأ في الاتصال بالخدمة" | خطأ عام | جرب الحلول أعلاه |

### خطوات التشخيص:

#### 1. تحقق من وحدة التحكم (Console)
```javascript
// افتح Developer Tools (F12)
// اذهب إلى تبويب Console
// ابحث عن رسائل خطأ حمراء
```

#### 2. تحقق من تبويب Network
```javascript
// في Developer Tools
// اذهب إلى تبويب Network
// حاول تحليل مرحلة
// تحقق من حالة الطلبات (Status)
```

#### 3. تحقق من الخادم
```bash
# في Terminal
# تأكد من أن الخادم يعمل على المنفذ 3000
# تحقق من عدم وجود أخطاء في Terminal
```

### حلول متقدمة:

#### 1. إعادة تثبيت التبعيات
```bash
cd frontend
rm -rf node_modules package-lock.json
npm install
npm run dev
```

#### 2. تحديث المتصفح
- تأكد من استخدام متصفح حديث
- جرب متصفح آخر (Chrome, Firefox, Edge)

#### 3. تعطيل الإضافات
- عطل إضافات المتصفح مؤقتاً
- جرب وضع التصفح الخفي

### إذا استمرت المشكلة:

#### 1. تحقق من سجلات الخادم
```bash
# في Terminal حيث يعمل الخادم
# ابحث عن رسائل خطأ
# انسخ رسائل الخطأ للمطور
```

#### 2. تحقق من إعدادات البيئة
```bash
# تأكد من وجود ملف .env.local
# تحقق من المتغيرات البيئية
```

#### 3. تواصل مع الدعم
```markdown
عند التواصل مع الدعم، قدم:
- رسالة الخطأ الكاملة
- نوع المتصفح والإصدار
- خطوات إعادة إنتاج المشكلة
- لقطات شاشة إذا لزم الأمر
```

### نصائح للوقاية:

#### 1. احتفظ بنسخة احتياطية من مفتاح API
#### 2. راقب استخدام API في Google AI Studio
#### 3. استخدم مفتاح API منفصل للتطوير
#### 4. احتفظ بسجل للأخطاء الشائعة

---

**ملاحظة**: معظم مشاكل الاتصال مؤقتة ويمكن حلها بإعادة المحاولة أو إعادة تشغيل الخادم.
